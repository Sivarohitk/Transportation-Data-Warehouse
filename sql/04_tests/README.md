# SQL Data Quality Checks

Use this folder for report-style SQL checks that support development and demo validation beyond the fail-fast test scripts under `tests/sql`.

Current report entry point:

- `00_data_quality_checks.sql`
  Batch summary, validation errors, reject summary, reconciliation metrics, restatement coverage, watermark status, and reporting-view samples.
